1、ArrayList类已经被Java在底层做了特殊处理，
打印对象不是地址值而是集合中存储的数据内容，在展示的时候会拿[]把所有的数据进行包裹
2、ArrayList类只能存储引用类数据类型，不能存储基本数据类型(但基本数据类型化为包装类即可被存储)
3、成员方法
add（）；remove（）；set（）；get（）；size（）；（具体看api文档）
4、打印该集合，自带有间隔符和括号