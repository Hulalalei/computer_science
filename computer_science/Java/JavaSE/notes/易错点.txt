1、一个类中如果没有写构造函数，则虚拟机自动提供；如果定义了构造，虚拟机将不再提供构造函数
2、键盘录入的两套体系尽量不要混用
3、==两边若是基本数据类型，则比较的是数据值；若是引用数据类型，则比较的是地址值
4、键盘录入的字符串，是new出来的
5、StringBuilder类中new出来的对象经过java的特殊处理，打印出来的是属性值而不是地址值
6、new出来的对象均为形参
7、java中char不能和数字进行运算，cpp可以