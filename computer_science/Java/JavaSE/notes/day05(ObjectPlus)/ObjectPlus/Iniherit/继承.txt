1、子类重写父类函数时，前面需要添加(@Override重写注释)以检查语法正确性
2、查看对象在内存中的结构：ClassLayout layout = ClassLayout . parseInstance(对象);
3、可以通过在空参构造中调用其他构造函数，(this(参数);)写在第一行
4、父类构造先调用，写在第一行，super();（可省略）
5、子类继承父类中protected的方法后，只能在类内调用，创建子类对象无法调用